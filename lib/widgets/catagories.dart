import 'package:flutter/material.dart';

class WidgetCatogories extends StatelessWidget {
  const WidgetCatogories({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('ji'),),
      
    );
  }
}